#include <stdio.h>
int main() //Program for difference of two numbers
{
                         int a;
                         int b;
                         printf("Enter two numbers:");
                         scanf("%d %d",&a,&b);
                         int Sum = a-b;
                         printf("Difference of two numbers is:%d",Sum);
                         return 0;
}